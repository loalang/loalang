namespace Fixtures/UndefinedReferences.

class X {
  public undefinedType: X c -> Y => c. // `Y` is undefined.

  public undefinedBehaviour: X c -> X =>
    c undefined. // `X` doesn't respond to `undefined`.

  public undefinedReference -> X => undefined. // `undefined` is undefined.

  public undefinedTypeDoesNotReportUndefinedBehaviour -> X =>
    undefined message. // `undefined` is undefined.
}